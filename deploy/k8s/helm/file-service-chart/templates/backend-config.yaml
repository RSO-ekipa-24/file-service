apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: files-backend-config
  namespace: essa-project
spec:
  healthCheck:
    type: HTTP
    port: 6868
    requestPath: /q/health/ready